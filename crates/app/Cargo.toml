[package]
name = "app"
version = "0.1.0"
edition = "2021"

[dependencies]
gpui = { git = "https://github.com/zed-industries/zed" }
dx-icon-search = { path = "../icon", default-features = false, features = ["rayon"] }
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"
walkdir = "2.5"
