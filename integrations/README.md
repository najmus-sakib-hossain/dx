# Instructions for demo purposes and will be removed later

This directory contains integration modules for various third-party services and platforms. Each integration is implemented as a separate Rust crate, allowing for modular development and easy maintenance.

```bash
git clone https://github.com/openclaw/openclaw && cd openclaw && rm -rf .git && cd ..
git clone https://github.com/ysz/recursive-llm recursive-llm && cd recursive-llm && rm -rf .git && cd ..
git clone https://github.com/alexzhang13/rlm rlm && cd rlm && rm -rf .git && cd ..
git clone https://github.com/alexzhang13/rlm-minimal rlm-minimal && cd rlm-minimal && rm -rf .git && cd ..
git clone https://github.com/openinterpreter/open-interpreter open-interpreter && cd open-interpreter && rm -rf .git && cd ..
git clone https://github.com/n8n-io/n8n && cd n8n && rm -rf .git && cd ..
git clone https://github.com/anomalyco/opencode && cd opencode && rm -rf .git && cd ..
git clone https://github.com/open-webui/open-webui && cd open-webui && rm -rf .git && cd ..
git clone https://github.com/Comfy-Org/ComfyUI comfy-ui && cd comfy-ui && rm -rf .git && cd ..
```

At the Integrations/OpenClaw folder, you can see that there are Google provider and there is also Google Auth provider giving Google Integrality Models. Please create a new ban project at root called chat and in that chat ban project, just use Click Prompt NPM package with Google Auth as exactly like integration OpenClaw Google implementation so that in the chat folder, we can chat with Google API key and Google Integrality Models directly with a nice UI and as fast as possible.

Please study the root integrations n18 folder and tell me how it has so much 500+ integration with other projects, MCP, and automation apps and websites. I'm a software engineer, so just give me a full overview of that source code. 
